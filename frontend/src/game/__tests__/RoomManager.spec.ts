import { describe, it, expect } from 'vitest'

// TODO: RoomManager tests need proper WebSocket mocking in Node.js environment
// These tests should be run in browser environment or with proper WebSocket polyfill
describe.skip('RoomManager', () => {
  it('should be tested in browser environment', () => {
    expect(true).toBe(true)
  })
})
